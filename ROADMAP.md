Path to v2:
1. UI & Persistence. Uncouple the current code from the MenuBarExtra, spawn a main macOS Window, and implement SwiftData so that the history you currently fetch is actually saved to disk.
2. Expanded Data. Update your fetch routines to also grab carbs and insulin data from the phone.
3. Charting. Rebuild the main dashboard chart to span 24 hours, injecting the new carb/insulin data points.